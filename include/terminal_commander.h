#ifndef TERMINAL_COMMANDER
	#define TERMINAL_COMMANDER
	#include <string>
	#include "pugixml.hpp"
	using namespace pugi;
	using namespace std;
	xml_document doc;
	int openDoc(string fileName);
#endif
